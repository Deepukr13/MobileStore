@model IEnumerable<string> 

@Html.ActionLink("All Phones", "Blist", "Product") 

@foreach (var link in Model) { 
    @Html.RouteLink(link, new { 

    controller = "Product", 

    action = "Blist", 

    categorys = link, 

    page = 1 
},
 
new { 

    @class = link == ViewBag.SelectedCategorys ? "selected" : null 
}) 
} 